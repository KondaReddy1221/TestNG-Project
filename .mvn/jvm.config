# Add this file if present in your project and contains JVM args like --enable-preview or --release
# I will remove or adjust those args.

# Removed any --enable-preview lines to avoid passing preview JVM args to Maven.
# (File intentionally left without --enable-preview)
